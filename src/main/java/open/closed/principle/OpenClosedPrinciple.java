package open.closed.principle;

public interface OpenClosedPrinciple
{
    // You should be able to extend a classes behavior, without modifying it
    // Objects should be open to extension, but closed to modification
    // When new methods need to be added on software, we should extend and not change the source code
}
